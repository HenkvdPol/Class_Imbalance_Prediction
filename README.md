Files that created the simulations and the results in figures. The figures are stored as separate zip-files to maintain the data storage. The file Setting/simulation.R runs the simulation given the parameters. The resulted Rdata files can be send upon request.
